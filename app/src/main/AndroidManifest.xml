<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adminaboukirhighinstitutes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentDetails" />
        <activity android:name=".StudentsList"></activity>
        <activity android:name=".NewStudentToFirebase" />
        <activity android:name=".Chose" />
        <activity android:name=".Semester" />
        <activity android:name=".EducationalYears" />
        <activity android:name=".InstitutesName" />
        <activity android:name=".InstituteLogo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>